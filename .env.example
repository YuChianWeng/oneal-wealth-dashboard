FINANCE_DB_PATH=
# Parent directory of FINANCE_DB_PATH; Docker mounts it read-only so SQLite WAL files are visible.
FINANCE_DATA_DIR=
OBSIDIAN_VAULT_PATH=
MARKET_DATA_DIR=
# Container-only history path is set by docker-compose.yml.
MARKET_HISTORY_DIR=
APP_TIMEZONE=Asia/Taipei
APP_ORIGIN=http://127.0.0.1:3003
PORT=3003
# Calendar days before confirmed investment cash is considered stale (positive integer).
INSIGHT_CASH_STALE_DAYS=7
